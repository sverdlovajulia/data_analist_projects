## «адача

÷елью проекта €вл€етс€ формирование модели монетизации компьтерной игры. Ѕазова€ модель предполагает, что реклама будет показыватьс€ после каждой постройки здани€ игроком. 

ƒл€ заказчика важно уменьшить количество рекламы в игре, не уйд€ при этом в минус. Ќеобходимо предложить альтернативную модель с меньшим количеством рекламы, котора€ будет окупатьс€.

»звестно, что каждый показ рекламы приносит 7 центов.

## ƒанные 

ƒатасет *game_actions.csv*:

- `event_datetime` Ч врем€ событи€;
- `event` Ч одно из трЄх событий:
    1. `building` Ч объект построен,
    2. `finished_stage_1` Ч первый уровень завершЄн,
    3. `project` Ч проект завершЄн;
- `building_type` Ч один из трЄх типов здани€:
    1. `assembly_shop` Ч сборочный цех,
    2. `spaceport` Ч космопорт,
    3. `research_center` Ч исследовательский центр;
- `user_id` Ч идентификатор пользовател€;
- `project_type` Ч тип реализованного проекта;

ѕомимо основного датасета есть два датасета с информацией о рекламных активност€х. ќни также помогут в решении задачи.

ƒатасет *ad_cost.csv* содержит колонки:

- `day` - день, в который был совершен клик по объ€влению
- `source` - источник трафика
- `cost` - стоимость кликов

ƒатасет user_source.csv содержит колонки:

- `user_id` - идентификатор пользовател€
- `source` - источников, с которого пришЄл пользователь, установивший приложение

## »спользуемые библиотеки
*pandas, seaborn, matplotlib, numpy, scipy*